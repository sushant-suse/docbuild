Added an in-tree PEP 517 build backend to automatically compile the Portal Schema from  RELAX NG Compact (``.rnc``) into XML (``.rng``) format during the build process, ensuring both formats are available in the distributed package.